/**
 * useAuth Kısayol Dosyası
 * 
 * Bu dosya, projede daha temiz bir import yapısı kurabilmek için
 * AuthContext dosyasında tanımlanmış olan `useAuth` hook'unu 
 * hooks klasörü altından tekrar dışa aktarır (Re-export).
 */
export { useAuth } from '../context/AuthContext';